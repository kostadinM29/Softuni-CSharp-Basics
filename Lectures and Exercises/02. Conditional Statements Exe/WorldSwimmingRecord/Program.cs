using System;

namespace WorldSwimmingRecord
{
    class Program
    {
        static void Main(string[] args)
        {
            //double record = double.Parse(Console.ReadLine());
            //double distance = double.Parse(Console.ReadLine());
            //double time = double.Parse(Console.ReadLine());

            //double swimTime = distance * time;
            //double addedResistance = 12.5 * Math.Floor(distance / 15);
            //double timeNeeded = swimTime + addedResistance;
            //if (timeNeeded < record)
            //{
            //    Console.WriteLine($"Yes, he succeeded! The new world record is {timeNeeded:F2} seconds.");
            //}
            //else
            //{
            //    double result = timeNeeded - record;
            //    Console.WriteLine($"No, he failed! He was {result:F2} seconds slower.");
            //}
            double b = 3;
            double a = 1 / b;
            Console.WriteLine($"{a}");
        }
    }
}
